PROMPT 09 — Dashboard, relatórios e e-mails
============================================
STATUS: [~] FEITO MVP ~85% — mailto + fila Firestore; SMTP/Trigger Email pendente. Exports CSV/PDF antecipados da Robusta

Cole no Cursor (Agent mode).
PRÉ-REQUISITO para envio real: extensão Trigger Email + SMTP/SendGrid (ver REQUISITOS-ACESSOS.txt).

---

Você está no Promoter Experience.

Estado atual:
- Dashboard com ciclo 20→19, KPIs, próximas visitas, tarefas pendentes.
- ReportsPage com exports CSV/PDF (NF, visitas do ciclo, recorrentes, gastos).
- Reset de senha Firebase + resumo de visita (mailto ou fila /mail).

Objetivo MVP:
1. [x] Dashboard: links para detalhe da visita; KPIs existentes mantidos.
2. [x] Botão "Enviar resumo por e-mail" no detalhe:
   - [x] monta texto (dados, datas, status, totais, contagens)
   - [x] destinatário no dialog
3. [x] Envio via fila Firestore + extensão Trigger Email (VITE_EMAIL_MODE=firestore).
4. [x] Sem modo firestore: abre mailto (sem fake success silencioso de API).
5. [x] Exports CSV/PDF intactos.

[ROBUSTA — não fazer agora]
- [ ] Templates rich de e-mail
- [ ] Envio automático por mudança de status
- [ ] Log completo de envios
- [ ] Gráficos avançados novos (relatórios já cobrem bastante)

Restrições:
- Não hardcodar API keys no frontend — fila Firestore + extensão.
- npm run build deve passar.

Entregáveis:
- Dashboard validado + fluxo de envio de resumo (mailto ou firestore).

---

Checklist manual:
[ ] KPIs batem com dados das páginas
[x] Dialog de resumo preenche certo (código)
[ ] E-mail chega via extensão (bloqueado: falta SMTP) — mailto funciona
[x] Exports CSV/PDF ainda funcionam

Notas:
- Configurar Trigger Email no Console + VITE_EMAIL_MODE=firestore + deploy rules.
