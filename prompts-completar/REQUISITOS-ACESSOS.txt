REQUISITOS DE ACESSOS, CHAVES E DECISÕES
========================================

Para completar o projeto de ponta a ponta, preciso (ou a equipe precisa ter pronto) o seguinte.

STATUS (código vs externo) — atualizado jul/2026
------------------------------------------------
[x] Código MVP principal implementado (visitas, agenda, CRM, tarefas, financeiro, docs, perfis)
[x] Mobile cards + scripts deploy/seed no repo
[x] E-mail: mailto + fila Firestore (extensão Trigger Email) no código
[x] Kanban tarefas + exports CSV/PDF (itens Robusta antecipados)
[~] Permissões MVP ~80% — rotas cliente e VisitorsPage pendentes (ver MVP-RELATORIO.md P1)
[~] Queries financeiro/documentos — equipe pode não ver dados do operador (ownerId)
[ ] Acessos/deploy Firebase ainda necessários para uso real
[ ] Provedor de e-mail (SMTP) ainda necessário para envio automático
[ ] Homologação formal 3 perfis + mobile — pendente

Avaliação completa: prompts-completar/MVP-RELATORIO.md

--------------------------------------------------------------------------------
1) FIREBASE (obrigatório)
--------------------------------------------------------------------------------
Projeto atual: borderless-e4a6a

Preciso que alguém com acesso de Owner/Editor no Firebase Console confirme:

[ ] Acesso ao Console Firebase do projeto borderless-e4a6a
[x] Firebase Authentication habilitado (Email/Password) — já em uso no app
[ ] Firestore em modo produção (ou regras já deployadas) — rules no repo, deploy pendente
[ ] Firebase Storage HABILITADO + rules deployadas — código pronto; enable/deploy pendente
[ ] Firebase Hosting com permissão de deploy
[ ] Conta com firebase-tools logado (firebase login)
[ ] Service Account JSON (Admin SDK) para:
    - setar custom claims (admin)
    - scripts de seed / convite
    - (se usar) Cloud Functions
    NÃO commitar esse JSON no git.

Arquivos locais esperados:
- [~] .env preenchido (base: .env.example) — confirmar no ambiente de cada dev
- (opcional) service-account.json em pasta ignorada pelo git

--------------------------------------------------------------------------------
2) E-MAIL TRANSACIONAL (obrigatório para MVP de comunicação)
--------------------------------------------------------------------------------
Hoje: reset de senha Firebase + resumo via mailto (padrão) ou fila /mail (VITE_EMAIL_MODE=firestore).
Para envio automático real, escolher UMA opção:

Opção A — Firebase Extension "Trigger Email" + provedor SMTP (recomendado; código pronto)
Opção B — Cloud Function + SendGrid / Resend / Amazon SES / Mailgun
Opção C — Integração com API do provedor que o cliente já usa

Preciso que vocês definam:
[ ] Qual provedor de e-mail usar
[ ] API Key / SMTP host, user, password
[ ] Domínio remetente verificado (ex.: noreply@promover.experience)
[ ] Lista de e-mails de teste autorizados

Sem isso, o módulo 09 fica em mailto (UI pronta, sem envio server-side).

--------------------------------------------------------------------------------
3) PERFIS E REGRAS DE NEGÓCIO (obrigatório — decisão, não chave)
--------------------------------------------------------------------------------
IMPLEMENTADO no código com matriz padrão:
- admin (claim) — acesso total
- user — dono das visitas (escrita)
- team — escrita nas visitas em teamMemberIds
- client — leitura nas visitas em clientUserIds

Preciso que confirmem (ou peçam ajuste):
[x] Nomes finais dos perfis (Admin / Equipe / Cliente) — adotados como admin/team/client
[x] Matriz padrão aplicada (client leitura; team/user escrita)
[x] Cliente via clientUserIds na visita
[x] Equipe via teamMemberIds por visita
[ ] Quem cria usuários: só Admin? convite por e-mail? — convite ainda não implementado

GAPS CONHECIDOS (código — P1):
[~] Bloqueio de rotas: só menu filtra cliente; URL direta ainda abre módulos
[~] VisitorsPage sem canWrite — cliente pode mutar CRM se acessar /visitantes
[~] Vínculo equipe/cliente por UID manual (sem seletor de usuários)

--------------------------------------------------------------------------------
4) FINANCEIRO — MODELO DE DADOS (decisão)
--------------------------------------------------------------------------------
[x] Mantido modelo atual de orçamentos/NF + anexos de comprovante (opção A)
[ ] OU migrar para despesas operacionais (categoria + comprovante)? — não feito
[ ] OU os dois (abas: Orçamentos | Despesas)? — não feito

Categorias padrão desejadas (se despesas):
[ ] lista (ex.: transporte, hotel, alimentação, brinde, outros) — N/A enquanto opção A

GAP CONHECIDO:
[~] listFinanceItems filtra por ownerId — equipe pode não ver linhas do operador na visita compartilhada

--------------------------------------------------------------------------------
5) DOCUMENTOS / STORAGE (obrigatório para módulo 08)
--------------------------------------------------------------------------------
[x] Código: upload/list/delete + storage.rules no repo
[ ] Firebase Storage habilitado + regras de Storage deployadas
[x] Tipos permitidos no código: PDF, JPG, PNG, WEBP
[x] Tamanho máximo: 10 MB
[x] Categorias: contrato, boarding, briefing, comprovante, outro
[x] Quem pode baixar: autenticado com acesso à visita (Firestore rules)

GAP CONHECIDO:
[~] storage.rules atuais: qualquer autenticado lê/escreve em visits/* — endurecer antes de produção
[~] listDocuments filtra por ownerId — mesmo problema da equipe que no financeiro

--------------------------------------------------------------------------------
6) AMBIENTES E DOMÍNIO (para deploy final)
--------------------------------------------------------------------------------
[ ] Ambiente de homologação separado? (outro projeto Firebase ou só Hosting preview)
[ ] Domínio final de produção (ex.: app.promover.experience)
[ ] DNS / acesso ao provedor de domínio, se for apontar Hosting custom domain
[ ] Contas de teste: 1 admin, 1 equipe, 1 cliente (e-mails reais)

--------------------------------------------------------------------------------
7) CONTEÚDO / COPY / BRANDING (opcional mas útil)
--------------------------------------------------------------------------------
[ ] Logo oficial e cores (se forem diferentes do que já está)
[ ] Texto padrão do e-mail de resumo da visita
[x] Checklist padrão de tarefas (DEFAULT_CHECKLIST em constants)

--------------------------------------------------------------------------------
8) O QUE NÃO PRECISO AGORA
--------------------------------------------------------------------------------
- Chave de OpenAI / outras IAs (não está no escopo)
- Stripe / pagamentos (não está no escopo)
- Push notifications mobile nativo (app é web)
- Acesso ao Figma (só se quiserem fidelidade pixel-perfect; dá para fechar MVP sem)

--------------------------------------------------------------------------------
RESUMO DO QUE AINDA BLOQUEIA O FECHAMENTO
--------------------------------------------------------------------------------
Já desbloqueado no código (~85–90% MVP):
1. [x] 3 perfis + matriz padrão
2. [x] Modelo financeiro opção A + comprovante
3. [x] Módulo documentos no código
4. [x] Kanban, relatórios CSV/PDF (Robusta antecipada)

Ainda bloqueante externo (P0):
1. Storage habilitado + deploy de rules/indexes
2. Contas de teste e quem será admin (UID para setar claim)
3. Deploy hosting (+ domínio se houver)
4. Homologação ponta a ponta (SETUP-EXTERNO.md §7)

Ainda bloqueante código (P1 — ver MVP-RELATORIO.md):
1. Rotas cliente + VisitorsPage canWrite
2. Queries financeiro/documentos para visita compartilhada
3. storage.rules mais restritivas

Opcional (P1 externo):
- Provedor de e-mail + credenciais (envio automático)

Robusta (P2 — ~7–10 dias adicionais):
- Convite usuários, templates visita, calendário agenda, brindes CRM
- Histórico alterações, permissões granulares, log e-mail, e2e/CI
