# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.firebase
*.local
# (removed for backup) .env
# (removed for backup) .env.local
credenciais.md
service-account.json
service-account*.json

# Cloud Functions
# (removed for backup) functions/.env
# (removed for backup) functions/.env.*
!functions/.env.example
functions/lib
functions/node_modules

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Force-track .env (user request)
!.env
!.env.*
!**/.env
!**/.env.*

# Secrets das Cloud Functions — nunca versionar (sobrescreve o force-track)
functions/.env
functions/.env.local
functions/.env.*
!functions/.env.example

